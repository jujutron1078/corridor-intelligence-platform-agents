READ_COMPANY_INFO_DESCRIPTION = """
Retrieve company information for the current organization (from runtime context).

Use this tool to get organization profile, capabilities, team bios/CVs, credentials,
and past projects relevant to proposal writing.

To avoid bloating chat context, prefer passing a `query` so the tool returns only the
exact information you need.
"""

